# ============================================================
# ChestX-Ray Detection - Requirements
# 
# ⚠️ ВАЖНО: PyTorch ДОЛЖЕН быть установлен ПЕРЕД этим файлом!
#
# Правильный порядок установки:
# 1. pip install --upgrade pip setuptools wheel
# 2. pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu130
# 3. pip install -r requirements.txt   <-- ТОЛЬКО ПОСЛЕ шага 2!
#
# ============================================================

# YOLOv8 (работает с уже установленным PyTorch)
ultralytics>=8.0.200

# Data processing
numpy==1.26.3
pandas>=2.0.0
opencv-python>=4.8.0

# Augmentation library
albumentations>=1.3.0

# Image processing (PIL для работы с изображениями)
Pillow>=10.0.0

# ML metrics and preprocessing
scikit-learn>=1.3.0
scipy>=1.11.0

# Utilities
tqdm>=4.66.0
matplotlib>=3.8.0
seaborn>=0.13.0
pyyaml>=6.0.0

# Development tools (optional)
black>=23.12.0
flake8>=6.1.0
isort>=5.13.0
